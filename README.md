# TransportationLPSolver
A PyQt desktop application for solving the Transportation Problem of Linear Programming using the Potentials (U-V) algorithm. Built with PyQt and NumPy for a responsive interface and fast computation.
